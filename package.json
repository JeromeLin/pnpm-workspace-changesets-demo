{
  "name": "pnpm-workspace-demo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "changeset": "changeset",
    "pre": "changeset pre enter next && changeset version && changeset publish",
    "release": "changeset && changeset version && pnpm install",
    "publish": "pnpm publish -r",
    "prepublish": "pnpm build",
    "deploy": "pnpm -r --filter=site run build",
    "build": "pnpm -r --filter=@keeljs/* run build",
    "test": "pnpm -r --filter=@keeljs/* run test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.23.2"
  }
}
